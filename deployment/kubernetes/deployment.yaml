kubernetes_deployment = """
apiVersion: apps/v1
kind: Deployment
metadata:
  name: finriskai-app
  namespace: finriskai
spec:
  replicas: 3
  selector:
    matchLabels:
      app: finriskai-app
  template:
    metadata:
      labels:
        app: finriskai-app
    spec:
      containers:
      - name: finriskai-app
        image: finriskai/app:latest
        ports:
        - containerPort: 8000
        env:
        - name: POSTGRES_HOST
          value: "postgres-service"
        - name: MONGODB_URI
          value: "mongodb://mongo-service:27017/finriskai"
        - name: REDIS_URL
          value: "redis://redis-service:6379/0"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: finriskai-service
  namespace: finriskai
spec:
  selector:
    app: finriskai-app
  ports:
  - port: 80
    targetPort: 8000
  type: LoadBalancer

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: finriskai-ingress
  namespace: finriskai
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - finriskai.example.com
    secretName: finriskai-tls
  rules:
  - host: finriskai.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: finriskai-service
            port:
              number: 80
"""